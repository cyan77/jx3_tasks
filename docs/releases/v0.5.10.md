# v0.5.10 本地与云端冲突保护

- 检测到本地和云端都产生改动时，自动暂停上传，避免覆盖任一方数据。
- 本地未同步状态会持久保存，应用重启后仍能识别待处理冲突。
- 自动保存本地冲突快照，云端原备份继续保留。
- 新增“保留本地并上传”“使用云端版本”“导出两份数据”三种处理方式。
- 保留本地时会创建新的云端备份，不覆盖原来的云端版本。
- 恢复冲突云端版本和直接恢复备份时增加确认提示，减少误操作。
- 修复同步初始化过程中关闭应用可能产生的异步状态错误。

> 本版本暂不自动合并冲突字段，遇到冲突时由用户明确选择，优先保证数据不丢失。
